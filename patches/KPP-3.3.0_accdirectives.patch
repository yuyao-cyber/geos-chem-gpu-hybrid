diff --git a/src/code.c b/src/code.c
index 804073e..2a3ab08 100644
--- a/src/code.c
+++ b/src/code.c
@@ -50,6 +50,11 @@ void (*GlobalDeclare)( int v );
 void (*InitDeclare)( int var, int nv, void * values );
 void (*DeclareConstant)( int v, char *val );
 void (*FunctionStart)( int f, int *vars );
+
+/* When set (by gen.c, under #ACCDIRECTIVES ON), the next emitted F90
+   function header is annotated with '!$acc routine seq'.  Cleared by
+   F90_FunctionStart after use. */
+int accRoutineSeqNext = 0;
 void (*FunctionPrototipe)( int f, ... );
 void (*FunctionBegin)( int f, ... );
 void (*FunctionEnd)( int f );
@@ -353,6 +358,15 @@ char *FUNCALL[] = { "FUN_SPLIT( Y, FIX, RCONST, Ydot, P, D )", /* index 0 = spli
   sprintf( tmp, " -e 's/KPP_NHESS/%d/g'", Hess_NZ );
   strncat( cmd, tmp, strlen(tmp)+1 );
 
+  // Lines holding the KPP_ACC_ROUTINE placeholder become '!$acc routine seq'
+  // under #ACCDIRECTIVES ON, and are deleted (byte-identical stock output)
+  // otherwise.
+  if ( useAccDirectives )
+    sprintf( tmp, " -e 's/^KPP_ACC_ROUTINE/  !$acc routine seq/g'" );
+  else
+    sprintf( tmp, " -e '/^KPP_ACC_ROUTINE/d'" );
+  strncat( cmd, tmp, strlen(tmp)+1 );
+
   // either CALL FUN or CALL FUN_SPLIT, depending on useAggregate:
   sprintf( tmp, " -e 's/KPP_FUN_OR_FUN_SPLIT/%s/g'", FUNCALL[useAggregate] );
   strncat( cmd, tmp, strlen(tmp)+1 );
diff --git a/src/code.h b/src/code.h
index e99acfe..848438c 100644
--- a/src/code.h
+++ b/src/code.h
@@ -203,6 +203,7 @@ extern void (*GlobalDeclare)( int v );
 extern void (*InitDeclare)( int v, int n, void * values );
 extern void (*DeclareConstant)( int v, char *val );
 extern void (*FunctionStart)( int f, int *vars );
+extern int accRoutineSeqNext;
 extern void (*FunctionPrototipe)( int f, ... );
 extern void (*FunctionBegin)( int f, ... );
 extern void (*FunctionEnd)( int f );
diff --git a/src/code_f90.c b/src/code_f90.c
index d1d7b35..9c87f4c 100644
--- a/src/code_f90.c
+++ b/src/code_f90.c
@@ -676,6 +676,10 @@ int narg;
     bprintf("%s ", varTable[ v ]->name );
   }
   bprintf(")\n");
+  if( accRoutineSeqNext ) {
+    bprintf("  !$acc routine seq\n");
+    accRoutineSeqNext = 0;
+  }
 
   FlushBuf();
 }
diff --git a/src/gdata.h b/src/gdata.h
index 8e970d8..e3e4e8b 100644
--- a/src/gdata.h
+++ b/src/gdata.h
@@ -196,6 +196,7 @@ extern int useLang;
 extern int useStochastic;
 extern int doFlux;
 extern int doAutoReduce;
+extern int useAccDirectives;
 extern int upperCaseF90;
 extern char f90Suffix[4];
 extern char minKppVersion[30]; // size [30] must be the same as in scanner.c
diff --git a/src/gen.c b/src/gen.c
index 13535b1..0e4bfb5 100644
--- a/src/gen.c
+++ b/src/gen.c
@@ -623,6 +623,7 @@ void GenerateStoichNum()
   if (useLang != MATLAB_LANG)  /* Matlab generates an additional file per function */
     UseFile( functionFile );
   CalcStoichNum = DefFnc( "CalcStoichNum", 1, "calculate stoichiometric numbers");
+  accRoutineSeqNext = useAccDirectives;
   FunctionBegin( CalcStoichNum, StoichNum );
   F90_Inline("  StoichNum(:,:) = 0.");
   for (i = 0; i < VarNr; i++) {
@@ -675,6 +676,7 @@ int F_VAR, FSPLIT_VAR;
   // Vdotout functionality can be accomplished using Vdot (hplin, 7/6/22)
   //
   // F90 needs Fun to have an extra argument for Aout (hplin, bmy, 30 Apr 2024)
+  accRoutineSeqNext = useAccDirectives;
   if( z_useAggregate ) {
     if (useLang == F90_LANG) {
       FunctionBegin( F_VAR, V, F, RCT, Vdot, Aout );\
@@ -693,7 +695,9 @@ int F_VAR, FSPLIT_VAR;
   if ( (useLang==MATLAB_LANG)&&(!z_useAggregate) )
      printf("\nWarning: in the function definition move P_VAR to output vars\n");
 
-  if ( useLang!=F90_LANG ) { /* A is a module variable in F90 */
+  /* Under #ACCDIRECTIVES the F90 scratch array A is declared per routine
+     (a module-level A would race between GPU threads). */
+  if ( useLang!=F90_LANG || useAccDirectives ) {
     NewLines(1);
     WriteComment("Local variables");
     Declare( A );
@@ -831,7 +835,9 @@ int F_VAR, FSPLIT_VAR;
     fprintf(functionFile, "! Haipeng Lin (hplin) - Apr 10 2022 KPP 3.0.0-AR\n");
     fprintf(functionFile, "!\n");
     fprintf(functionFile, "! ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~\n\n");
-    fprintf(functionFile, "SUBROUTINE Fun_SPLITF ( V, F, RCT, P_VAR, D_VAR, Aout )\n\n");
+    fprintf(functionFile, "SUBROUTINE Fun_SPLITF ( V, F, RCT, P_VAR, D_VAR, Aout )\n");
+    if( useAccDirectives ) fprintf(functionFile, "  !$acc routine seq\n");
+    fprintf(functionFile, "\n");
     fprintf(functionFile, "! V - Concentrations of variable species (local)\n");
     fprintf(functionFile, "  REAL(kind=dp) :: V(NVAR)\n");
     fprintf(functionFile, "! F - Concentrations of fixed species (local)\n");
@@ -844,6 +850,10 @@ int F_VAR, FSPLIT_VAR;
     fprintf(functionFile, "  REAL(kind=dp) :: D_VAR(NVAR)\n");
     fprintf(functionFile, "!### Aout - Array for returning KPP reaction rates for diagnostics\n");
     fprintf(functionFile, "  REAL(kind=dp), OPTIONAL :: Aout(NREACT)\n");
+    if( useAccDirectives ) {
+      fprintf(functionFile, "! A - Rate for each equation\n");
+      fprintf(functionFile, "  REAL(kind=dp) :: A(NREACT)\n");
+    }
 
     NewLines(1);
     WriteComment("Computation of equation rates");
@@ -1189,8 +1199,10 @@ int Jac_SP, Jac;
                   "the Jacobian of Variables in sparse matrix representation");
   Jac     = DefFnc( "Jac", 4, "the Jacobian of Variables");
 
-  if( useJacSparse )
+  if( useJacSparse ) {
+    accRoutineSeqNext = useAccDirectives;
     FunctionBegin( Jac_SP, V, F, RCT, JVS );
+  }
   else
     FunctionBegin( Jac, V, F, RCT, JV );
 
@@ -1977,6 +1989,7 @@ int dim;
   UseFile( linalgFile );
 
   SOLVE = DefFnc( "KppSolve", 2, "sparse back substitution");
+  accRoutineSeqNext = useAccDirectives;
   FunctionBegin( SOLVE, JVS, X );
 
   for( i = 0; i < VarNr; i++) {
@@ -3346,12 +3359,23 @@ case 'h':
       F90_Inline("  USE %s_Precision", rootFileName );
     }
     else {
-      if( doAutoReduce ) F90_Inline("  USE %s_Global, only : DO_FUN", rootFileName );
+      if( doAutoReduce && !useAccDirectives )
+        F90_Inline("  USE %s_Global, only : DO_FUN", rootFileName );
       F90_Inline("  USE %s_Parameters", rootFileName );
     }
-    F90_Inline("  IMPLICIT NONE\n", rootFileName );
-    Declare( A ); /*  mz_rs_20050117 */
-    if ( useLang == F90_LANG || useLang == F77_LANG ) WriteOMPThreadPrivate(" A "); /* msl_20160419 */
+    if ( doAutoReduce && useAccDirectives ) {
+      /* compile-time mask: lets the compiler drop the IF(DO_FUN(i)) tests */
+      F90_Inline("  IMPLICIT NONE" );
+      F90_Inline("  LOGICAL, PARAMETER :: DO_FUN(NVAR) = .TRUE.\n" );
+    } else {
+      F90_Inline("  IMPLICIT NONE\n", rootFileName );
+    }
+    if ( !useAccDirectives ) {
+      /* module-level scratch A would race between GPU threads; under
+         #ACCDIRECTIVES it is declared locally in each routine instead */
+      Declare( A ); /*  mz_rs_20050117 */
+      if ( useLang == F90_LANG || useLang == F77_LANG ) WriteOMPThreadPrivate(" A "); /* msl_20160419 */
+    }
     F90_Inline("\nCONTAINS\n\n");
 
   UseFile( rateFile );
@@ -3390,12 +3414,15 @@ case 'h':
       F90_Inline("  USE %s_Precision", rootFileName );
     }
     else {
-      if( doAutoReduce ) F90_Inline("  USE %s_Global, ONLY: DO_JVS", rootFileName);
+      if( doAutoReduce && !useAccDirectives )
+        F90_Inline("  USE %s_Global, ONLY: DO_JVS", rootFileName);
       F90_Inline("  USE %s_Parameters", rootFileName );
     }
     if ( useJacSparse )
       F90_Inline("  USE %s_JacobianSP\n", rootFileName);
     F90_Inline("  IMPLICIT NONE", rootFileName );
+    if ( doAutoReduce && useAccDirectives )
+      F90_Inline("  LOGICAL, PARAMETER :: DO_JVS(LU_NONZERO) = .TRUE.");
     F90_Inline("\nCONTAINS\n\n");
 
   if ( useStoicmat ) {
@@ -3436,11 +3463,17 @@ case 'h':
 
    UseFile( linalgFile );
     F90_Inline("MODULE %s_LinearAlgebra\n", rootFileName);
-    if( doAutoReduce ) F90_Inline("  USE %s_Global, ONLY: DO_SLV", rootFileName);
+    if( doAutoReduce && !useAccDirectives )
+      F90_Inline("  USE %s_Global, ONLY: DO_SLV", rootFileName);
     F90_Inline("  USE %s_Parameters", rootFileName );
     if ( useJacSparse )
       F90_Inline("  USE %s_JacobianSP\n", rootFileName);
     F90_Inline("  IMPLICIT NONE", rootFileName );
+    if ( doAutoReduce && useAccDirectives ) {
+      /* module copy of the mask, device-visible via 'declare copyin' */
+      F90_Inline("  LOGICAL, SAVE :: DO_SLV(NVAR+1) = .TRUE.");
+      F90_Inline("  !$acc declare copyin(DO_SLV)");
+    }
     F90_Inline("\nCONTAINS\n\n");
 
    UseFile( utilFile );
diff --git a/src/scan.h b/src/scan.h
index 280aa99..90a4e4f 100644
--- a/src/scan.h
+++ b/src/scan.h
@@ -119,6 +119,7 @@ void CmdStochastic( char *cmd );
 void CmdStoicmat( char *cmd );
 void CmdFlux( char *cmd );
 void CmdAutoReduce( char *cmd );
+void CmdAccDirectives( char *cmd );
 void CmdUpperCaseF90( char *cmd );
 void CmdMinVersion( char *cmd );
 int EqNoCase( char *s1, char *s2 );
diff --git a/src/scan.l b/src/scan.l
index dffee53..e55a667 100644
--- a/src/scan.l
+++ b/src/scan.l
@@ -337,6 +337,7 @@ STOICH  {LIT}[a-zA-Z_0-9]*[*]
                          { "UPPERCASEF90",  PRM_STATE, UPPERCASEF90 },
                          { "MINVERSION",    PRM_STATE, MINVERSION   },
                          { "AUTOREDUCE",    PRM_STATE, AUTOREDUCE   },
+                         { "ACCDIRECTIVES", PRM_STATE, ACCDIRECTIVES },
                          { 0,               0,         0            }
                        };
 
diff --git a/src/scan.y b/src/scan.y
index 3a869d1..9906fdf 100644
--- a/src/scan.y
+++ b/src/scan.y
@@ -95,6 +95,7 @@
 %type <str> TPTID USEID
 %type <str> rate eqntag
 %token FLUX AUTOREDUCE
+%token ACCDIRECTIVES
 %token      PLSPC
 %type <str> PLSPC
 %token UPPERCASEF90
@@ -186,6 +187,8 @@ section	        : JACOBIAN PARAMETER
 		  { CmdFlux( $2 ); }
                 | AUTOREDUCE PARAMETER
 		  { CmdAutoReduce( $2 ); }
+                | ACCDIRECTIVES PARAMETER
+		  { CmdAccDirectives( $2 ); }
                 | UPPERCASEF90 PARAMETER
 		  { CmdUpperCaseF90( $2 ); }
                 | MINVERSION PARAMETER
diff --git a/src/scanner.c b/src/scanner.c
index 06ca034..0540d83 100644
--- a/src/scanner.c
+++ b/src/scanner.c
@@ -108,6 +108,7 @@ int useLang        = F77_LANG;
 int useStochastic  = 0;
 int doFlux         = 0;
 int doAutoReduce   = 0;
+int useAccDirectives = 0;
 int useDeclareValues = 0;         // if useValues=1 KPP replaces parameters
                                   // like NVAR etc. by their values in vector
                                   // or matrix declarations
@@ -362,6 +363,19 @@ void CmdAutoReduce( char *cmd )
   ScanError("'%s': Unknown parameter for #AUTOREDUCE [ON|OFF]", cmd );
 }
 
+void CmdAccDirectives( char *cmd )
+{
+  if( EqNoCase( cmd, "OFF" ) ) {
+    useAccDirectives = 0;
+    return;
+  }
+  if( EqNoCase( cmd, "ON" ) ) {
+    useAccDirectives = 1;
+    return;
+  }
+  ScanError("'%s': Unknown parameter for #ACCDIRECTIVES [ON|OFF]", cmd );
+}
+
 void CmdUpperCaseF90( char *cmd )
 {
   if( EqNoCase( cmd, "OFF" ) ) {
diff --git a/util/sutil.f90 b/util/sutil.f90
index 69052c3..e0691c7 100644
--- a/util/sutil.f90
+++ b/util/sutil.f90
@@ -1,6 +1,7 @@
 
 ! ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 SUBROUTINE KppDecomp( JVS, IER )
+KPP_ACC_ROUTINE
 ! ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
 !        Sparse LU factorization
 ! ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
